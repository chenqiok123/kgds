# 抽卡系统 · 接口预留规范 v1

> 日期：2026-08-13　状态：接口预留（下一版本实现游戏功能，当前只留钩子，不影响现有功能）
> 目标：用户点亮的"小球"可用来抽卡——每个知识大类 = 一个卡池 + 一个积分池；点亮程度和数量决定积分，积分抽卡（R/SR/SSR/UR），卡牌有电子版和实体版，每张卡对应一项技能，用于未来实景剧本杀道具。

---

## 一、核心概念映射

| 游戏概念 | 系统概念 | 现状 |
|:---|:---|:---|
| 小球 | 知识图谱节点（node） | ✅ 已有 81 个节点 |
| 点亮 | 节点掌握（mastered，confidence≥0.67） | ✅ 已有 `node_status` |
| 点亮程度 | confidence（掌握度 0~1） | ✅ 已有 |
| 知识大类 | `category` 字段 | ✅ 本次已加（11 类） |
| 卡池 | 按 category 分组的卡牌集合 | ⬜ 预留 |
| 积分池 | 按 category 分组的积分余额 | ⬜ 预留 |
| 卡牌 | R/SR/SSR/UR，含技能 | ⬜ 预留 |

---

## 二、数据模型（下一版本建表，先定 schema）

### 1. 卡池 `card_pool`
```
pool_id      TEXT  PK    -- 用 category 标识（如 "寿险年金"）
name         TEXT        -- 卡池名
description  TEXT
```

### 2. 卡牌 `card`
```
card_id       TEXT  PK
pool_id       TEXT        -- 所属卡池（=category）
rarity        TEXT        -- 'R' | 'SR' | 'SSR' | 'UR'
title         TEXT        -- 卡牌名
skill         TEXT        -- 对应的一项技能（剧本杀道具的关键属性）
skill_desc    TEXT        -- 技能描述/效果
digital_asset TEXT        -- 电子版资源（图片/动效 URL 或资产 ID）
physical_flag INTEGER     -- 是否支持实体版（0/1）
```

### 3. 积分池 `points_pool`
```
user_id  INTEGER
pool_id  TEXT             -- 大类
balance  INTEGER          -- 积分余额
PRIMARY KEY (user_id, pool_id)
```

### 4. 点亮事件 `node_light_event`（★ 数据输出接口核心）
```
event_id   INTEGER PK AUTOINCREMENT
user_id    INTEGER
node_id    TEXT           -- 小球
category   TEXT           -- 知识大类（决定进哪个积分池）
confidence REAL           -- 点亮程度 0~1
mastered   INTEGER        -- 是否点亮 0/1
delta      REAL           -- 相比上次的掌握度变化（正=进步）
created_at TEXT
```
> 积分公式未来从这张表推导，任何公式（程度×数量×大类权重）都能回溯计算。

### 5. 用户卡牌 `user_card`
```
user_id          INTEGER
card_id          TEXT
obtained_at      TEXT
form             TEXT   -- 'digital' | 'physical'
physical_status  TEXT   -- 'none' | 'pending' | 'shipped'（实体版发货状态）
```

### 6. 抽卡记录 `draw_log`
```
user_id   INTEGER
pool_id   TEXT
card_id   TEXT
rarity    TEXT
cost      INTEGER   -- 消耗积分
drawn_at  TEXT
```

---

## 三、数据输出接口（点亮 → 积分系统的输入）

**触发点**：用户完成诊断（`/api/submit`）或学习，节点掌握度变化时。

**约定**：`/api/submit` 返回的 `node_status` 每个条目必须带 `category` 字段：

```json
{
  "node_status": {
    "L1-01": {
      "label": "寿险产品结构",
      "layer": "foundation",
      "category": "寿险年金",          // ← 本次新增，必带
      "correct": 3, "total": 4,
      "confidence": 0.75,
      "mastered": true
    }
  }
}
```

**未来落库**：服务端在 submit 时，把 `node_status` 中 `confidence` 有变化的节点，写成一条 `node_light_event`（含 `delta`）。

---

## 四、数据输入接口（积分 → 抽卡系统）

> 当前全部返回 `501 {"code":"NOT_READY","message":"抽卡系统下一版本开放"}`，仅预留路由。

| 方法 | 路径 | 用途 | 请求 | 响应（未来） |
|:---|:---|:---|:---|:---|
| GET | `/api/gacha/pools` | 卡池列表 | — | `[{pool_id,name,description,card_count}]` |
| GET | `/api/gacha/points` | 我的积分池 | — | `[{pool_id,balance}]` |
| GET | `/api/gacha/pools/{id}/cards` | 卡池内卡牌 | — | `[{card_id,rarity,title,skill}]` |
| POST | `/api/gacha/draw` | 抽卡 | `{pool_id}` | `{card_id,rarity,title,skill,is_new}` |
| GET | `/api/gacha/my-cards` | 我的卡牌 | — | `[{card_id,rarity,title,skill,form}]` |
| GET | `/api/cards/{id}` | 卡牌详情 | — | `{card_id,rarity,title,skill,skill_desc,digital_asset,physical_flag}` |
| POST | `/api/cards/{id}/physical` | 申请实体版 | — | `{physical_status}` |

---

## 五、知识大类（category）现状

本次已为 81 个节点标注 `category`（默认按主题聚类，可后续合并）：

| 大类 | 节点数 | 大类 | 节点数 |
|:---|:--:|:---|:--:|
| 产品 | 15 | 高客信托 | 6 |
| 销售技能 | 11 | 数字化品牌 | 5 |
| 财务法税 | 11 | 健康意外医疗 | 4 |
| 客户经营 | 9 | 团队增员 | 4 |
| 合规风控 | 8 | 产业资源 | 2 |
| 寿险年金 | 6 | | |

> 11 个卡池偏多，产品上线前可合并（如"寿险年金+健康意外医疗"→"保障产品"）。改 `category` 值即可，接口不变。

---

## 六、稀有度与技能（预留约定，下一版本细化）

- **稀有度**：R / SR / SSR / UR 四档，概率权重与保底机制下一版本设计。
- **技能**：每张卡 `skill` 字段承载"一项技能"，是剧本杀道具的核心属性——卡牌价值 = 技能稀缺度，建议 UR 卡对应高阶技能（如"高客面谈"、"税务筹划"），R 卡对应基础技能。
- **电子版 / 实体版**：`digital_asset` 承载电子版（可立即获得）；实体版经 `POST /api/cards/{id}/physical` 走发货流程（`physical_status`）。
