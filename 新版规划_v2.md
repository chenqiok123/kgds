# 兔扑·知识图谱诊断 App — 新版规划 v2

> 日期：2026-08-13　状态：P1 已完成（段位制 + 错题解析）+ P2 核心完成（toast + 2D 图谱降级）
> 范围：KGDS（诊断/图谱）+ 兔扑（伴读/打擂）合并统一

---

## 零、北极星：产品要回答的三个问题

**如果我是用户，我愿意用这套系统提升自己吗？有趣吗？有用吗？**

当前版本诚实回答：**只用一次（诊断完即弃）**。它是一台"体检仪"，不是"健身教练"——只有测评、没有变强闭环，模块割裂、无复访钩子、冷启动劝退。

新版价值主张：**从"诊断仪"到"变强引擎"**。一句话定位：

> **每天10分钟，看着你的知识图谱从红变绿。**

- **有趣** = 游戏化：光球养成（81个光球亲手点亮，灰→红→黄→金）、擂台段位（青铜→王者）、每日一题、进步曲线。
- **有用** = 刻意练习闭环：诊断→补强任务→错题间隔重复→再诊断验证，看到图谱肉眼可见地更绿。
- **复访钩子** = 连续打卡 + "还有N个光球待点亮" + "这周你变强了多少"。

> 所有功能设计（含问题4上下游、问题5错题解析、问题1打擂）都服务于这条主线：
> **问题4 上下游 = 让知识成网（真懂，而非孤立记忆）；问题5 错题解析 = 错题追着你直到掌握；问题1 打擂 = 先有"人设的成长"，擂台才不空。**

---

## 零点五、段位制：取代"从业年限"的能力锚点

**核心转变：从"自报年限"到"系统段位"。** 用户不填从业几年，诊断完直接得到一个段位，系统告诉他"你已相当于从业X年的老手"。段位是整个系统的唯一能力锚点，驱动选题、打擂、推荐、晋级。

**为什么比自报年限好**：年限是"自己说的"（可虚报、静态），段位是"系统评的"（客观、动态、可晋级）。

### 段位体系（草案 6 段）

| 段位 | 称号 | 相当于从业 | 评定标准（分层掌握率） |
|:---|:---|:---|:---|
| 🥉 青铜 | 新人 | 0–1年 | 基础层 <60% |
| 🥈 白银 | 熟手 | 1–3年 | 基础层 ≥60% |
| 🥇 黄金 | 骨干 | 3–5年 | 基础≥75% 且 提升≥50% |
| 💎 铂金 | 老手 | 5–10年 | 基础≥85% 且 提升≥70% |
| 👑 钻石 | 高手 | 10年+ | 提升≥85% 且 升华≥60% |
| 🌟 王者 | 专家 | 15年+ | 升华≥80% |

> 关键：段位由**分层掌握率**决定，非总分——避免"刷基础题刷出高段位"。

### 三个机制

1. **评定**：首次诊断完按分层掌握率定段，报告页标出"🥇 黄金 · 相当于从业4年"。
2. **晋级**：再诊断/光球点亮达阈值 → 晋级仪式（动画+徽章+"你已晋升 💎 铂金，相当于从业8年"）——最强复访钩子。
3. **驱动选题**：青铜默认基础层切入、错多下探；王者基础题压缩、主攻升华+错题盲区。

### 首次诊断定难度：CAT 自适应

不再问年限 → 第一次无段位依据 → 用 **CAT（计算机自适应测验）**：中等难度起步，答对升难度、答错降难度，一套题跑完自然测出真实水平直接定段（比自报更准，无法虚报）。

### 段位 = 变强引擎的中枢

```
诊断(CAT) → 定段 → 选题自适应 → 光球点亮/错题掌握 → 图谱变绿
                                    ↓
                          达阈值 → 🎉晋级 → 再诊断验证 → 打擂同段匹配
```

- 光球点亮 = 段位进度条；打擂 = 同段匹配（青铜不撞王者）；错题解析 = 老手错题才是真盲区。

---

## 一、现状问题清单（根因已定位）

| # | 问题 | 根因 | 严重度 |
|:-:|:---|:---|:---:|
| 1 | 打擂总排名无数据 | 本地/云端割裂 + 登录依赖 + 错误静默吞掉 | 🔴 高 |
| 2 | 四页无返回首页导航 | 两个独立HTML，无统一导航壳 | 🔴 高 |
| 3 | 查看原始图谱无法返回 | toggle 实现，非"按住显示/松开返回" | 🟡 中 |
| 4 | 光球知识太简单 | 只弹 alert(content)，未推导上下游 | 🟡 中 |
| 5 | 内部知识无错题解析 | 报告只做节点聚合，题目无 explanation 字段 | 🟡 中 |
| 6 | alert() 做知识展示 | 原生弹窗体验差 | 🟢 低 |
| 7 | 错误全部静默 | catch 吞异常，无法区分网络错误/空数据 | 🟡 中 |
| 8 | 登录态割裂 | kgds_user vs topoo_* 两套不互通 | 🔴 高 |

---

## 二、目标架构

**统一单页应用（SPA）+ 底部 5 Tab 导航**：

```
┌─────────────────────────────────────────┐
│  🏠 首页    🗺️ 图谱    📖 今日    👥 书友    👤 我的  │  ← 底部固定导航
└─────────────────────────────────────────┘
```

| Tab | 内容 | 来源 |
|:---|:---|:---|
| 首页 | 诊断入口 + 今日概览 + 我的成绩速览 | 合并原 app.html「诊断」 |
| 图谱 | 3D知识图谱 + 光球详情（上下游） | app.html「图谱」 |
| 今日 | 每日伴读 + 期数进度 | topoo.html「今日」 |
| 书友 | 打擂排名 + 留言 + 期数榜 | topoo.html「书友」 |
| 我的 | 个人中心 + 历史诊断 + 设置 | topoo.html「我的」 |

**首页 = 返回锚点**：任何Tab都能通过底部导航回到首页，解决问题2。

---

## 三、分阶段实施计划

### P0 — 修Bug（问题1/2/3，1~2天）✅ 已完成 2026-08-13

1. **统一登录态**：KGDS 和兔扑共用同一 token（都存 `localStorage.kgds_user`），去掉 `topoo_token` 双轨。✅
2. **修复打擂排名链路**：
   - `submitEpisodeQuiz` 提交前未登录 → 弹登录引导，登录后自动继续提交（`pendingSubmit` 桥接）。✅
   - `fetchBookRanking` 去掉 `catch(e){return []}`，网络错误抛出、由调用处显示「排名加载失败」，与「暂无数据」区分。✅
3. **统一导航壳**：两套页面底部统一为 5 Tab（诊断/图谱/今日/书友/我的），跨页跳转（`/arena?tab=xxx` ↔ `/?tab=graph`），每页可回首页。✅
4. **原始图谱改按住**：`btn-full-graph` 由 `onclick toggle` 改为 `onpointerdown`（`enterFullGraph`）+ `onpointerup/onpointerleave/onpointercancel`（`exitFullGraph`），文案「👁️ 按住看完整图谱」。✅

> 附带修复：`server.py` `/api/learning-tips` 用了未定义的 `BASE_DIR`（应为 `ROLE_DIR`），原会导致 500，已修复。

### P1 — 功能补齐（问题4/5，2~3天）

5. **光球上下游知识**：
   - 预计算每个节点的**上游**（入边 source，即"它依赖什么"）和**下游**（出边 target，即"它支撑什么"），带边 label。
   - 点击光球 → 弹**详情卡片**（替换 alert），三段式：`知识点简介` + `⬆️ 上游知识` + `⬇️ 下游知识`。
6. **错题解析** ✅：
   - 题目数据补 `explanation` 字段（tests.json / product_tests.json，或从 node.content 自动兜底）。→ 已用 `node.content` 自动兜底。
   - `/api/submit` 返回逐题结果（题目、你的答案、正确答案、对错、解析）。→ 已加 `item_results` 字段。
   - `renderReport` 新增「📝 错题解析」模块，展开可看每道错题的解析（市场竞争 + 内部知识**统一**）。→ 已完成。
7. **段位制（取代从业年限）** ✅：
   - 前端去掉 `f-years` 自报字段，改为「首次诊断后展示段位」。→ 已删 `f-years`，改为「诊断后自动评定段位」提示。
   - 后端加 `compute_rank(node_status/layer_stats)`：按分层掌握率定段（见"零点五"段位表）。→ 已完成，`/api/submit` 返回 `rank`。
   - 首次诊断用 **CAT 自适应**（答对升难、答错降难），一套题跑完定段。→ 简化为「覆盖三层均衡出题定段」（严格逐题 CAT 留待下版）。
   - 晋级机制：再诊断达阈值 → 晋级仪式（动画+徽章+"相当于从业X年"）。→ 已完成（`checkRankUp` + `showRankUpModal`）。
   - 段位驱动选题难度（青铜基础切入、王者攻升华+错题）。→ 留待下版（段位已入历史记录，可驱动后续选题）。

### P2 — 体验优化（问题6/7 + 移动端，3~4天）

7. **弹窗组件化**：统一底部抽屉/卡片替代 `alert()`（知识详情、错题解析、确认框）。→ 已完成（toast + 抽屉卡片，知识详情用 `showNodeDetail` 抽屉）。
8. **错误提示规范化** ✅：所有 fetch 加 toast，区分"网络异常/无数据/权限"。→ 已完成 `toast()`，关键 alert 已替换为 toast（区分 error/success/info）。
9. **3D图谱降级** ✅：移动端 8 秒超时 → 降级为 2D 力导向图（保光球可点、保上下游详情）。→ 已完成 `renderGraph2D()`（分层 SVG 布局，节点可点，移动端/3D库不可用直接 2D，桌面端 8s 超时降级）。
10. **数据一致性**：打擂成绩、诊断记录统一走服务器，localStorage 只做离线缓存。→ 部分完成（诊断/打擂已走服务器，localStorage 作离线兜底）。

---

## 四、验收标准

- [x] 登录一次，诊断 + 打擂 + 排名三处身份一致
- [x] 打擂提交后，总排名实时出现自己的名次
- [x] 5个Tab任意切换都能回首页
- [x] 图谱「按住查看原始图谱」松开立即恢复真实状态
- [x] 点光球能看到 简介 + 上游 + 下游 三块
- [x] 内部知识诊断结果有「错题解析」，与市场竞争一致
- [x] 断网时明确提示"网络异常"，不再显示"暂无数据"误导
- [x] 不再要求自报从业年限；首次诊断后自动展示段位 + "相当于从业X年"
- [x] 再诊断达阈值时触发晋级仪式，段位可见可成长

---

## 五、风险与备注

- 合并 SPA 涉及 app.html/topoo.html 两套 DOM 结构重排，改动量大，建议 P0 先做「导航壳 + 登录统一」，其余渐进迁移。
- 题目 explanation 字段补全工作量大（380 + 168 题），可先用 node.content 自动兜底，后续人工精修。
- 3D→2D 降级需保留原 3D 在桌面端的体验，按设备能力判断。

---

## 六、下一版本预告：抽卡系统（已预留接口）

用户点亮的小球 → 抽卡：每个知识大类 = 一个卡池 + 一个积分池；点亮程度和数量决定积分，积分抽卡（R/SR/SSR/UR），卡牌有电子版和实体版，每张卡对应一项技能（用于未来实景剧本杀道具）。

**本次已完成的接口预留**（详见 `抽卡系统_接口预留_v1.md`）：

1. **数据输出**：`nodes.json` 的 81 个节点已加 `category` 字段（11 个知识大类）；`/api/submit` 返回的 `node_status` 已带 `category`（点亮事件可追溯到卡池）。
2. **数据输入**：`server.py` 已预留 `/api/gacha/*` 与 `/api/cards/*` 路由桩，当前返回 `501 NOT_READY`。
3. **schema 定稿**：卡池/卡牌/积分池/点亮事件/用户卡牌/抽卡记录 六张表的字段已定义。

下一版本只需：建表 + 填卡牌数据 + 实现积分公式 + 抽卡概率，接口无需再改。
